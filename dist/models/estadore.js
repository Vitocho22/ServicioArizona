"use strict";
/*
import { DataTypes } from "sequelize";
import sequelize from '../db/connection';

// Definición del modelo EstadoRe
export const EstadoRe = sequelize.define('EstadoRe', {
    id: {
        type: DataTypes.INTEGER,
        primaryKey: true,
        autoIncrement: true
    },
    estado: {
        type: DataTypes.STRING,
        allowNull: false,
        unique: true
    }
});

// Sincronización del modelo con la base de datos
EstadoRe.sync();
*/ 
