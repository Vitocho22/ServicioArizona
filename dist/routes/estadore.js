"use strict";
/*import { Router } from "express";
import { createEstadoRe, deleteEstadoRe, getEstadosRe , getEstadoReById, updateEstadoRe } from "../controllers/estadore";

const router = Router();

// Rutas para operaciones CRUD de Estado
router.post('/estadoRe', createEstadoRe);
router.get('/estadoRe', getEstadosRe);
router.get('/estadoRe/:id', getEstadoReById);
router.put('/estadoRe/:id', updateEstadoRe);
router.delete('/estadoRe/:id', deleteEstadoRe);

export default router;*/ 
