"use strict";
/*import { Router } from "express";
import { createEstado, getEstado, getEstadoById, updateEstado, deleteEstado } from "../controllers/estado";

const router = Router();

// Rutas para operaciones CRUD de Estado
router.post('/estado', createEstado); // Crear un nuevo estado
router.get('/estado', getEstado); // Obtener todos los estados
router.get('/estado/:id', getEstadoById); // Obtener un estado por ID
router.put('/estado/:id', updateEstado); // Actualizar un estado por ID
router.delete('/estado/:id', deleteEstado); // Eliminar un estado por ID

export default router;*/ 
